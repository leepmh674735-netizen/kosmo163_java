package com.winter.io.ex1;

import com.winter.util.ex1.StudentDTO;

public class SchoolMain {

	public static void main(String[] args) {
		StudentInfo si = new 
			

	}while(true)

	{
      
		
		
		
		try {
			FileReader fileReader = new FileReader(file);
			BufferedReader br = new BufferedReader
			String s=null;
			while((s = br.readLine()) != null) {
			String [] ar = s.split("-");
			
			for(int i=0;i,ar.length;++) {
			       
				String [] ar = s.split("-");
				StudentDTO studentDTO = new StudenDTO();
				studentDTO.setName(ar[0]);
				studentDTO.setName(Integer.parseInt(ar[1]));
				studentDTO.setName(Integer.parseInt(ar[2]));
			    studentDTO.setName(Integer.parseInt(ar[3]));
			    list.add(studentDTO);
			    
			    
			}
			catch (Exception e) {
			// TODO Auto-generated catch block
			
		}
	}

}
